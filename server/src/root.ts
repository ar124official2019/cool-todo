/**
 * exports referrence to the root directory of the project.
 * It is only logically effective if the file is residing in the root directory
 *  of given project
 */

const root = __dirname;
export default root;
